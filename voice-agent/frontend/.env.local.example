# Frontend Debug Configuration
# Copy this file to .env.local and modify as needed
# This file is preserved across dev-local and build-frontend runs

# Enable verbose debug logging in browser console (works in both dev and production)
# Logs: WebSocket connections, audio processing, AWS credentials, session tracking
VITE_WS_DEBUG=false
